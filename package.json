{
  "name": "hono-flare",
  "scripts": {
    "dev": "pnpm db:migrate:preview && wrangler dev --remote",
    "deploy": "pnpm db:migrate:prod && wrangler deploy --minify",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings",
    "lint": "biome lint .",
    "check": "biome check --write .",
    "db:generate": "drizzle-kit generate",
    "db:migrate:preview": "wrangler d1 migrations apply db-flare --remote --preview",
    "db:migrate:prod": "wrangler d1 migrations apply db-flare --remote",
    "studio:preview": "drizzle-kit studio --config=drizzle.config.ts",
    "studio:prod": "TARGET=prod drizzle-kit studio --config=drizzle.config.ts"
  },
  "simple-git-hooks": {
    "pre-commit": "pnpm run check"
  },
  "dependencies": {
    "@hono/oauth-providers": "^0.6.2",
    "@hono/zod-validator": "^0.4.3",
    "drizzle-orm": "^0.36.4",
    "hono": "^4.6.12"
  },
  "devDependencies": {
    "@biomejs/biome": "1.9.4",
    "@cloudflare/workers-types": "^4.20241127.0",
    "@types/node": "^22.10.1",
    "better-sqlite3": "^11.6.0",
    "dotenv": "^16.4.5",
    "drizzle-kit": "^0.28.1",
    "simple-git-hooks": "^2.11.1",
    "tsx": "^4.19.2",
    "wrangler": "^3.88.0"
  }
}
